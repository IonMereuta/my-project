name: TEST2

on:
  workflow_dispatch:

permissions:
  id-token: write
  contents: read

jobs:
  stg-cd:
    name: STG
    uses: ./.github/workflows/test.yml
    secrets:
      health_safety_url: ${{ secrets.TEST_SECRET }}
